name: ResinDB Final Object Staging

on:
  push:
    branches: [main]
    paths-ignore:
      - '.github/.resindb-v320-delta/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Confirm staging state
        run: echo 'ResinDB final-object transport staging is active; no branch is created.'
