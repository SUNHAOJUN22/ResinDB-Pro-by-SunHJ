name: One-time direct similarity exact-tree final validation

on:
  push:
    branches: [main]

permissions:
  contents: write
  actions: write
  statuses: write

concurrency:
  group: one-time-direct-similarity-final-validation
  cancel-in-progress: false

jobs:
  validate-final-tree:
    if: contains(github.event.head_commit.message, 'run direct similarity exact-tree final validation')
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Check out main
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: main
          fetch-depth: 0
          token: ${{ github.token }}

      - name: Delete merged branch and prepare exact final tree
        shell: bash
        run: |
          set -euo pipefail
          branch='agent/direct-similarity-radar-20260803'
          if git ls-remote --exit-code --heads origin "refs/heads/${branch}" >/dev/null 2>&1; then
            git push origin --delete "$branch"
          fi
          git ls-remote --heads origin | tee /tmp/resindb-pass7-final-branch-proof.txt
          test "$(wc -l < /tmp/resindb-pass7-final-branch-proof.txt | tr -d ' ')" -eq 1
          test "$(grep -c 'refs/heads/main$' /tmp/resindb-pass7-final-branch-proof.txt || true)" -eq 1
          git rm .github/workflows/one-time-direct-similarity-final-validation.yml
          git rm .github/run-direct-similarity-final-validation.trigger
          tested_tree=$(git write-tree)
          printf 'TESTED_TREE=%s\n' "$tested_tree" >> "$GITHUB_ENV"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install exact dependencies
        shell: bash
        run: |
          set -euo pipefail
          : > /tmp/npm-ci.log
          cp package.json /tmp/resindb-package-exact.json
          cp package-lock.json /tmp/resindb-package-lock-exact.json
          restore_manifests() {
            cp /tmp/resindb-package-exact.json package.json
            cp /tmp/resindb-package-lock-exact.json package-lock.json
          }
          trap restore_manifests EXIT
          node scripts/prepare-ci-manifest.mjs | tee /tmp/ci-manifest-normalization.json
          for registry in https://registry.npmjs.org https://registry.npmmirror.com; do
            for attempt in 1 2 3; do
              printf '\nregistry=%s attempt=%s\n' "$registry" "$attempt" | tee -a /tmp/npm-ci.log
              if npm ci --registry="$registry" >> /tmp/npm-ci.log 2>&1; then
                restore_manifests
                trap - EXIT
                exit 0
              fi
              sleep $((attempt * 10))
            done
          done
          exit 1

      - name: Complete exact-tree validation
        run: npm run validate:ci

      - name: Record dependency audits and freshness
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          set +e
          npm audit --omit=dev --audit-level=high --json > artifacts/npm-audit-prod.json
          prod_code=$?
          npm audit --audit-level=high --json > artifacts/npm-audit-all.json
          all_code=$?
          npm outdated --json > artifacts/npm-outdated.json
          outdated_code=$?
          set -e
          if [ ! -s artifacts/npm-outdated.json ]; then printf '{}\n' > artifacts/npm-outdated.json; fi
          printf '%s\n' "$outdated_code" > artifacts/npm-outdated-exit-code.txt
          test "$prod_code" -eq 0
          test "$all_code" -eq 0

      - name: Prove exact final repository and bundle source tree
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          git ls-remote --heads origin | tee artifacts/branch-proof.txt
          test "$(wc -l < artifacts/branch-proof.txt | tr -d ' ')" -eq 1
          test "$(grep -c 'refs/heads/main$' artifacts/branch-proof.txt || true)" -eq 1
          test "$(git write-tree)" = "$TESTED_TREE"
          test -s src/services/mathUtils.ts
          test -s src/components/charts/SimilarProductsRadar.tsx
          test -s src/components/features/Product/ProductDetailDrawer.tsx
          test -s tests/unit/mathUtils.test.ts
          test -s docs/CODE_MATH_PERFORMANCE_AUDIT_2026-08-03_PASS7_DIRECT_SIMILARITY_RADAR.md
          grep -q 'parseFiniteNumericValue' src/services/mathUtils.ts
          grep -q 'buildNormalizedComparisonProfile' src/services/mathUtils.ts
          grep -q 'sharedFeatureCount' src/components/features/Product/ProductDetailDrawer.tsx
          grep -q '覆盖率约束的直接牌号推荐' README.md
          tar --sort=name --mtime='UTC 1970-01-01' --owner=0 --group=0 --numeric-owner --mode='u+rwX,go+rX,go-w' \
            -czf artifacts/exact-source-tree.tar.gz \
            .github data docs native schemas scripts src tests .npmrc .nvmrc eslint.config.js index.html \
            package-lock.json package.json README.md tsconfig.json vite.config.ts
          sha256sum artifacts/exact-source-tree.tar.gz | tee artifacts/exact-source-tree.sha256

      - name: Write final validation context
        shell: bash
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const tests = JSON.parse(fs.readFileSync('artifacts/test-results.json', 'utf8'));
          fs.writeFileSync('artifacts/ci-context.json', `${JSON.stringify({
            schemaVersion: 10,
            repository: process.env.GITHUB_REPOSITORY,
            triggerSha: process.env.GITHUB_SHA,
            testedTree: process.env.TESTED_TREE,
            runId: Number(process.env.GITHUB_RUN_ID),
            testFiles: tests.testResults.length,
            testCount: tests.numTotalTests,
            finalBranchCount: 1,
            validationScope: 'direct-similarity-normalized-radar-exact-tree',
            generatedAt: new Date().toISOString(),
          }, null, 2)}\n`);
          fs.writeFileSync('artifacts/ci-gates.json', `${JSON.stringify({
            schemaVersion: 8,
            status: 'PASS',
            testedTree: process.env.TESTED_TREE,
            gates: [
              'strict-numeric-parsing','sparse-minmax-statistics','coverage-adjusted-direct-similarity',
              'minimum-shared-dimensions','stable-similarity-order','normalized-comparison-radar',
              'docs','source-hygiene','canonical-governed-data','compute-surface','scientific-ui',
              'lint','typescript','regression','isolated-unit','isolated-science-workers','whole-source-coverage',
              'build','http-smoke','chromium-ui','production-audit','complete-audit',
              'single-main-branch','deterministic-exact-source-tree',
            ],
          }, null, 2)}\n`);
          NODE

      - name: Generate validation receipt and reports
        shell: bash
        run: |
          set -euo pipefail
          npm run validate:compute
          npm run report:receipt
          npm run report:validation
          npm run report:pdf

      - name: Render PDF for visual acceptance
        shell: bash
        run: |
          set -euo pipefail
          pdf='artifacts/ResinDB-Pro-3.2.0-Final-Validation-Report.pdf'
          test -s "$pdf"
          if ! command -v pdfinfo >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y poppler-utils
          fi
          pdfinfo "$pdf"
          pdftoppm -png -r 120 "$pdf" artifacts/pdf-render >/dev/null
          test "$(find artifacts -maxdepth 1 -name 'pdf-render-*.png' | wc -l)" -ge 2

      - name: Upload final validation evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: resindb-direct-similarity-final-evidence-${{ github.sha }}
          path: |
            coverage/
            artifacts/
            README.md
            src/services/mathUtils.ts
            src/components/charts/SimilarProductsRadar.tsx
            src/components/features/Product/ProductDetailDrawer.tsx
            tests/unit/mathUtils.test.ts
            docs/CODE_MATH_PERFORMANCE_AUDIT_2026-08-03_PASS7_DIRECT_SIMILARITY_RADAR.md
            FINAL_CODE_AUDIT.md
            FINAL_RELEASE_REPORT.md
          if-no-files-found: error
          retention-days: 30

      - name: Commit the tested final tree
        shell: bash
        run: |
          set -euo pipefail
          expected=$(printf '%s\n' \
            '.github/run-direct-similarity-final-validation.trigger' \
            '.github/workflows/one-time-direct-similarity-final-validation.yml')
          actual=$(git diff --cached --name-only)
          test "$actual" = "$expected"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'chore: finalize direct similarity exact-tree validation'
          test "$(git rev-parse 'HEAD^{tree}')" = "$TESTED_TREE"
          git push origin HEAD:main
          printf 'FINAL_SHA=%s\n' "$(git rev-parse HEAD)" >> "$GITHUB_ENV"

      - name: Publish final commit validation status
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          payload=$(jq -nc \
            --arg state success \
            --arg target_url "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" \
            --arg description 'ResinDB direct-similarity exact-tree validation: success' \
            --arg context 'resindb/ci-evidence' \
            '{state:$state,target_url:$target_url,description:$description,context:$context}')
          curl -fsSL -X POST \
            -H 'Accept: application/vnd.github+json' \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${FINAL_SHA}" \
            -d "$payload" >/dev/null
